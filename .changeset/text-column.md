---
"@driftdev/buoy": patch
---

Buoys and the dashed "never mentioned" line sit in the article's text column, measured from its content, instead of spanning a root (such as `<main>`) that is wider than the article.
